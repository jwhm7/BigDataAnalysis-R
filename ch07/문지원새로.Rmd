---
title: "Exercise 7"
author: "문지원"
date: '2021 4 29 '
output: html_document
---
# Galton Height Data
## 아버지와 아들의 키의 회귀식을 구해보고 의미를 파악해보시오

### (1) 아버지와 아들의 키의 회귀식 
```{r}
df <- galton %>% 
   filter(Gender== 'M') %>% 
   select(Father, Height)
head(df)

df_m <- df %>% 
   mutate(father= Father*2.54, son= Height*2.54)
head(df_m)

model <- lm(son ~ father, data= df_m)
coef(model)
plot(df_m$father, df_m$son, xlab= 'father(cm)', ylab= 'son(cm)') +
abline(model, col= 'red', lwd =3)
```


### (2) 회귀식의 의미 분석
```{r}
summary(model)
```
Ans) p-value가 0.05보다 작으므로 H0.
